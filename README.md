# trying to make an amazing API w/ express

## init project

Install `node.js`

```
npm init

npm init -y
```

Create directory `src/`
Create file `src/index.js`

Install `yarn`

```bash
npm i -g yarn
```

Add `Express` dependency:

```
npm i [...]

yarn add [...]

yarn add express
```
